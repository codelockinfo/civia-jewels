{% schema %}
{
  "name": "Image With Text",
  "settings": [
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Latest Collections"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Timeless Elegance With Modern Luxury"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Experience the Renaissance reborn in each brilliant diamond. Browse our curated collections that blend modern elegance with timeless beauty."
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "url",
      "id": "image_link",
      "label": "Select collection"
    }
  ],
  "presets": [
    {
      "name": "Image With Text",
      "category": "Custom"
    }
  ]
}
{% endschema %}
<div class="container">
  <div class="image-text-section" style="display: flex; align-items: center; gap: 40px; padding: 60px 0;">
    <div class="text" style="flex: 1;">
      <div class="subheading">{{ section.settings.subheading }}</div>
      <div class="heading">{{ section.settings.heading }}</div>
      <div class="desc">{{ section.settings.description }}</div>
    </div>
    <div class="image" style="flex: 1;">
      {% if section.settings.image %}
        {% if section.settings.image_link != blank %}
          <a href="{{ section.settings.image_link }}">
            <img
              src="{{ section.settings.image | image_url }}"
              alt="Image"
            >
          </a>
        {% endif %}
      {% endif %}
    </div>
  </div>
</div>

<style>
  .container{
    max-width:1400px;
  }

    .image-text-section {
    display: flex;
    align-items: center;
    gap: 40px;
    padding: 60px 0;
  }

  .image-text-section .text {
    flex: 1;
  }

  .image-text-section .image {
    flex: 1;
  }

  .image-text-section img {
    max-width: 100%;
    /* border-radius: 8px; */
  }
  .subheading{
    font-size:26px;
    margin-bottom: 30px;
    color: #000000;
  }

  .heading{
    font-family: Kaisei Decol, serif;
    font-size: 60px;
    line-height: 1;
    margin-bottom: 20px;
    font-weight: 500;
  }
  .desc{
    font-size: 24px;
    line-height: 1.1;
  }
</style>

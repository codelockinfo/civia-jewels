{% schema %}
{
  "name": "Video With Text Block",
  "settings": [
    {
      "type": "video",
      "id": "video_file",
      "label": "Upload Video File"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Customize Your Perfect Diamond Ring & More With Us"
    },
    {
      "type": "textarea",
      "id": "text",
      "label": "Description",
      "default": "Shop your ideal diamond ring with Renaissance Jewel’s expert craftsmen. Collaborate with our experts to create a piece of custom diamond jewelry that's truly unique to you."
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button Text",
      "default": "Contact Us"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link",
    }
  ],
  "presets": [
    {
      "name": "Video With Text Block",
      "category": "Custom"
    }
  ]
}
{% endschema %}


<div class="custom-video-text">
  <div class="custom-video">
    {% if section.settings.video_file != blank %}
      <video autoplay muted loop playsinline>
        <source src="{{ section.settings.video_file | file_url }}" type="video/mp4">
        Your browser does not support the video tag.
      </video>
    {% endif %}
  </div>

  <div class="custom-text">
    <h2>{{ section.settings.heading }}</h2>
    <p>{{ section.settings.text }}</p>
    {% if section.settings.button_link != blank and section.settings.button_text != blank %}
      <a href="{{ section.settings.button_link }}" class="custom-button">{{ section.settings.button_text }}</a>
    {% endif %}
  </div>
</div>
